<?php
    $savedJobsContainer = new \Laminas\Session\Container('gastro24_savedjobs');
?>

<?php if (!$this->isExpired && !$this->preview) : ?>
    <?php if (isset($savedJobsContainer->jobs[$this->jobId])) : ?>
        <button type="button" class="btn btn-default saved <?= $this->buttonCss?>"
                data-text-saved="<?=$this->translate('Marked')?>" data-text-save="<?=$this->translate('Mark')?>" data-job="<?= $this->jobId ?>">
            <span class="hidden-xsds"><?=$this->translate('Marked')?></span>
        </button>
    
    <?php else : ?>
        <button type="button" class="btn btn-default <?= $this->buttonCss?>"
                data-text-saved="<?=$this->translate('Marked')?>" data-text-save="<?=$this->translate('Mark')?>" data-job="<?= $this->jobId ?>">
            <span class="hidden-xsds"><?=$this->translate('Mark')?></span>
        </button>
    <?php endif ?>
<?php endif ?>